"use client";
import UploadForm from "@/components/UploadForm";

const Home: React.FC = () => {
  return (
    <div className="pt-16 border flex justify-center">
      <UploadForm />
    </div>
  );
};

export default Home;
